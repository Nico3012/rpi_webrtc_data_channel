# Keep everything up to date
To ensure, we are compatible with the latest version, just ask AI, if `startupTemplate`, `mainTemplate` and `cleanupTemplate` inside `main.go` are using the commands `ip`, `hostapd` and `dnsmasq` correctly in the latest spec.
