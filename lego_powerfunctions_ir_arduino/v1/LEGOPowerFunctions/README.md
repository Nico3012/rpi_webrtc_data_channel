# Changelog:
`legopowerfunctions.cpp`:
replaced `cli()` with `noInterrupts()`
replaced `sei()` with `interrupts()`

removed: `legopowerfunctions.h~`
removed: `legopowerfunctions.o`
removed: `examples` directory
